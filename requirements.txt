# Pinned so a rebuild reproduces the image these versions were tested against.
# To upgrade: change a pin deliberately, rebuild, and re-run the test suites.
#
# Direct dependencies
Flask==3.1.3
Flask-Login==0.6.3
gunicorn==26.0.0
openpyxl==3.1.5
xlrd==2.0.2
Pillow==12.3.0
qrcode[pil]==8.2
reportlab==5.0.0

# Transitive dependencies, pinned for reproducibility
blinker==1.9.0
charset-normalizer==3.4.9
click==8.4.2
et-xmlfile==2.0.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.2
Werkzeug==3.1.8
